apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sentiment-api.fullname" . }}-test-connection"
  labels:
    {{- include "sentiment-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sentiment-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
